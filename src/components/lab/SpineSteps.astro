---
// The pipeline explainer: Problem -> Brief -> Lab -> Report. Used on the home.
const steps = [
  { k: 'Problem', v: 'A real, production-shaped problem from a founder, operator, or engineer, logged in the Problem Bank.' },
  { k: 'Brief', v: 'The problem, scoped: constraints, success criteria, and a public bar for what counts as solved.' },
  { k: 'Lab', v: 'A hands-on build against that Brief, scored against the public bar, at a workshop or on your own.' },
  { k: 'Report', v: 'The teardown of what was built and what it scored. The builder keeps the proof.' },
];
---
<ol class="spine">
  {steps.map((s, i) => (
    <li class="step">
      <span class="num">{String(i + 1).padStart(2, '0')}</span>
      <h3 class="k">{s.k}</h3>
      <p class="v">{s.v}</p>
    </li>
  ))}
</ol>

<style>
  .spine {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(auto-fit, minmax(11.5rem, 1fr));
  }
  .step {
    border: 1px solid var(--border);
    border-radius: var(--radius);
    background: var(--bg-2);
    padding: 1.3rem 1.4rem;
  }
  .num { font-family: var(--font-mono); font-size: 0.75rem; color: var(--accent); }
  .k { margin: 0.5rem 0 0.4rem; font-size: 1.05rem; }
  .v { margin: 0; font-size: 0.95rem; color: var(--text-2); line-height: 1.6; }
</style>
